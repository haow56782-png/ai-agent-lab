# Paper Formatter — Development environment overrides
#
# Local dev: Vite dev server proxies /api → localhost:4000
# Leave VITE_API_BASE_URL empty to use Vite proxy.
#
# VITE_API_BASE_URL=http://localhost:4000
